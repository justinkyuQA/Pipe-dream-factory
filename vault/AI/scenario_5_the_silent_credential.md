# Ai Note

Scenario 5: The Silent Credential

A valid credential exists that is never used interactively and never fails authentication. It generates no alerts because it behaves exactly as designed.
Thought Experiment: How do you monitor for misuse of correct behavior, rather than incorrect behavior?