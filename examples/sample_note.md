# Example Note

Pipe Dream Factory converts notes into structured books.

This sample exists so new users can immediately test the pipeline.

Add notes to the vault, run the pipeline, and review the generated output.
